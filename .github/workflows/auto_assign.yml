name: Auto Assign Issue

on:
  issues:
    types: [opened]

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest

    steps:
      - name: Assign issue based on dropdown
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |

            const issue = context.payload.issue;
            const body = issue.body;

            let assignee = null;

            if (body.includes("Anisat")) {
              assignee = "USERNAME_ANISAT";
            } else if (body.includes("Toi")) {
              assignee = "Messaouda-H";
            }

            if (assignee) {
              await github.rest.issues.addAssignees({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                assignees: [assignee]
              });

              console.log("Assigné à:", assignee);
            }